1. 很好的地方
	1. 先講 黑盒子 (how to use)
	2. 再講 證明
2. 這次不懂下次要講的
	1. master theorem
		1. 用 recursion tree 的 leaf-heavy 跟 root-heavy
			1. 怎麼跟 最剛開始的 master thm. $T(n)=aT(n/b)+f(n)$ 串起來 ?
				1. 中間的 推導 過程
		2. 限制(不能用 master theorem 的情況)
			1. 像是我這次舉的 $f(n) = 2^n$ 其實可以用 master thm.
				1. 所以 master theorem 倒底哪些情況不能使用 (exponential) ?
		3. case 2: 這個 k 哪來的?![[Pasted image 20260912112211.png]]
	 2. akra-bazzi (genearl 的 master theorem)
		1. 可以講證明的話 (陳想知道，我是覺得講這個就一定要講到證明)
	3. matrix multiplication (divide and conquer 的 ex) 要講
	4. 去研究一下 `快速冪` 是甚麼(陳覺得會考) $\rightarrow$ 用 matrix 去加速 recursion
		1. 這次交大就有 fast doubing 的問題 O(logn)
	5. dp
		1. 這個要找例子講
	6. 驗證一下這次交大那個每個數的加法(big number 相加)
		1. 到底是 O(1) 還是 O(n)
3. 許願(下次要講的)
	1. 時間複雜度的定義
		1. big-O, little-O, bit-omega 的定義 (這次我大概懂，但是沒有連貫起來)
		2. 接著配 比較 complexity 的題目 (陳想知道台大的題目)
			1. 比如說 $O(e^na^n)$ & $O(logn)$ 之間的大小 ?
				1. 就是要彼此比較 (先從已知的先排順序，再把不知道的排)
					1. 可以用 lim 的方式去知道大小
	2. sorting 的 best/worst case 時間複雜度 (sorting 的部分應該就是有了解到就可以拿到分數的)
		1. bobble sort
			1. best case(只要一個 iteration)
			2. worst case
		2. quick sort 
			1. best case: 最前，最後
			2. worst case: ?
			3. (我自己想的) 所以，pivot 的挑選很重要
		3. merge sort
	3. comparsion basd 的 sort 最快為 O(nlogn) 的證明
		1. 用 decision tree 去證明
	4. tree
		1. 有哪些 tree ? (binary tree, bst, red-black tree(我想講的), heap)
		2. ... (留到下次說)
4. 講題目方式 要更正
	1. 陳發現我跟別人讀題目的方式不太一樣，而且有時候會讀錯
	2. 而且我會預設題目大家都看過了
	3. 所以，每次講題目之前，要先帶題目
